import { Meta, Source } from '@storybook/addon-docs';
import { Typography } from '.';
import { VariantMapping } from './constant'


<Meta title="Typography" />

# Typography
<div style={{display:'flex', flexDirection: 'column', gap: '10px', margin: '30px 0'}}>
  {
    Object.keys(VariantMapping).map((variant) => 
      <Typography 
        key={variant} 
        variant={variant}
      >
        {variant}
      </Typography>
    )}
</div>

## Example

<Typography variant='Heading1'>Heading1</Typography>
<Typography>Body with <a>Link</a></Typography>
<Source code={`
<Typography variant='Heading1'>Heading1</Typography>
<Typography>Body with <a href="#">Link</a></Typography>
`}/>
